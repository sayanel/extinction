// @author: Alex
using UnityEngine;
using System.Collections;

namespace Extinction {
    namespace Enums{
        enum UnitBehavior {
            Idle,
            Moving,
            Attacking
        }

        enum PlayerType {
            Survivor,
            Herbie
        }

        enum AmmoType {
            Default
        }
    }
}